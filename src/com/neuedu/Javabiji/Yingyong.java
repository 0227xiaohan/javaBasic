package com.neuedu.Javabiji;

public class Yingyong {
    public static void main(String[] args) {
        //引用数据类型
        //int  s   float  b:只能存放一个
        //存放一组数据     多个用数组
        //    int[]  a={1,2,3,4,5};//一个数组，一组数
        //   int[]  a1=new int[5];
        //  int[] a2=new int[]{1,2,3,4,5,6};//以上三行是在java中数组的三种写法
        //  int a3[]={}; //java中这句一般不使用，但也不报错
        //索引下标
        //    a.length:用来获取数组长度
        //数组里面的下标（index）从0开始；
        //冒泡排序：数组里的数字从小到大的排序
     /*   int[] a = {23, 26, 25, 27};//对他排序，首先考虑排多少次，相邻的数先进行比较

        for (int i = 0; i < a.length-1; i++) { //控制次数
   for(int j=0;j<a.length-1;j++){
       //数据类型
       int n=0;
       if(a[j]>a[j+1]){//a里面的前一个和后一个比大小，
           a[j]=a[j+1];//j+1:数组前后顺序互换
           int tem=0;
           tem=a[j];
           a[j]=a[j+1];//获取数组中的某一项的值
         a[j+1]=tem;
       }
   }
        }        //控制次数
    }
}*/
//foreach:循环
//for(int a:arrs){                  }
//(a:当前项，

//数组下标越界异常：ArrayIndexOutoffBoundsExceptoon
//可以解决
//数组里的长度不可变，程序运行过程中长度不可改变


//引用类型：数组（若没给初始化的值有默认的初始值为0）
//整数类型初始值为0
//小数类型初始值为0.0
//double初始值为false
//char类型初始值为空格
//string初始值为null

        //java是个面向对象的语言，在java中做一些操作，对象进行的操作
        //首先有类才会有对象：类是一组事物的特征的总结，
        // 学生类    抽象概念性了解，把他反映到java上的表示，
//可以理解为具有相同特征和行为的一类事物，
//




    }
}
